# Block all direct web access to this directory (it lives inside the web root
# on shared hosting like InfinityFree). Works on Apache 2.2 and 2.4.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
